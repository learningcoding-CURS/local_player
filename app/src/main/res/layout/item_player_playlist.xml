<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingEnd="12dp"
    android:paddingTop="6dp"
    android:paddingBottom="6dp"
    android:background="@drawable/selector_panel_item">

    <!-- Thumbnail with fallback icon overlay (size set by adapter from settings) -->
    <FrameLayout
        android:id="@+id/framePanelThumb"
        android:layout_width="72dp"
        android:layout_height="52dp"
        android:background="#1AFFFFFF">

        <ImageView
            android:id="@+id/imgPanelThumb"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:visibility="gone" />

        <!-- Fallback: music/film icon shown while loading or when no thumb available -->
        <ImageView
            android:id="@+id/imgPanelThumbFallback"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_playlist"
            android:tint="#44FFFFFF" />

        <!-- Index badge (bottom-right corner of thumbnail) -->
        <TextView
            android:id="@+id/tvPanelItemIndex"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="2dp"
            android:textColor="#CCFFFFFF"
            android:textSize="10sp"
            android:background="#66000000"
            android:paddingStart="3dp"
            android:paddingEnd="3dp"
            android:fontFamily="monospace" />

    </FrameLayout>

    <!-- File name -->
    <TextView
        android:id="@+id/tvPanelItemName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="8dp"
        android:textColor="@color/textPrimary"
        android:textSize="12sp"
        android:maxLines="2"
        android:ellipsize="end"
        android:lineSpacingMultiplier="1.2" />

</LinearLayout>
